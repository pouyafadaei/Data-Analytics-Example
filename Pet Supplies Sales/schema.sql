CREATE TABLE pet_supplies
(
    product_id      VARCHAR PRIMARY KEY,
    category        VARCHAR,
    animal          VARCHAR,
    size            VARCHAR,
    price           VARCHAR,
    sales           VARCHAR,
    rating          VARCHAR,
    repeat_purchase VARCHAR
);
